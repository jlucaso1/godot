// Never executed. The entry point only exists so the project publishes as an app, which
// is what triggers the native build and the interop table generation.
internal static class Program
{
    private static void Main() { }
}
